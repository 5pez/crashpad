import PropertyPageCarousel from "./PropertyPageCarousel";

export default function PropertyPage({ match }) {
  return (
    <div>
      <h1>PropertyPage</h1>
      <p>This is a demo of the PropertyPage component.</p>
    </div>
  );
}
