{
  "routes": [
    {
      "method": "GET",
      "path": "/pads",
      "handler": "pads.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pads/count",
      "handler": "pads.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/pads/:id",
      "handler": "pads.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/pads",
      "handler": "pads.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/pads/:id",
      "handler": "pads.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/pads/:id",
      "handler": "pads.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
